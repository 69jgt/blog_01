1) Instalamos decouple: pip install python-decouple
2) Creamos el archivo .env en el mismo directorio que settings.py. En este archivo vamos a tener los datos que deseamos ocultar:
    SECRET_KEY=akbmdlp32hm1!x5yqjcwkaghw4$x(d3wdn$j1uaxo61s7w(cv4
    DEBUG=True
    DB_ENGINE=django.db.backends.postgresql
    DB_NAME=new_blog
    DB_USER=blog
    DB_PASSWORD=1zzf123x
    EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
    EMAIL_HOST=smtp.gmail.com
    EMAIL_USE_TLS=True
    EMAIL_PORT=587
    EMAIL_HOST_USER=tutmotsis69@gmail.com
    EMAIL_HOST_PASSWORD=mibitvrtarzjbzpw

    NOTA- ES IMPORTANTE NO DEJAR ESPACIOS ENTRE EL IGUAL Y EL VALOR DE LAS VARIABLES

3) En settings.py realizamos los siguientes cambios:
    3.1) Importamos config de decouple: from decouple import config
    3.2) Vamos sustituyendo aquellas variables que contengan datos sensibles, p.e.:
        SECRET_KEY = config('SECRET_KEY')
        DEBUG = config('DEBUG', cast=bool) # Devuelve un string y lo pasamos a booleano
        EMAIL_PORT = config('EMAIL_PORT', cast=int) # Devuelve un string y lo pasamos a entero    